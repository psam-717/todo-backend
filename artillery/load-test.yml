# artillery: typescript
# npx artillery run artillery/load-test.yml
config:
 target: "http://localhost:4000"
 phases:
 - duration: 60
   arrivalRate: 10
   rampTo: 50
scenarios:
 - name: "Get all todos"
   flow:
    - get:
       url: "/api/todos/all"
       expect: 
        - statusCode: 200
